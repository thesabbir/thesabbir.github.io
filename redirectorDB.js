/**
 * All links NEED http:// in-front to work
 * Links with special chars need to be in single quotes
 * redirectorDB is just a javascript object so js syntax applies but be consistent.
 *
 */

var redirectorDB = {
    "blog": "https://thesabbir.com"
};

